# ===========================================
# SystemsCheck Backend Environment Variables
# ===========================================

# Server Configuration
NODE_ENV=development          # development | production
PORT=3002                     # API server port

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Option 1: Individual connection parameters (development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=systemscheck
DB_USER=your_username
DB_PASSWORD=your_password

# Option 2: Connection string (production - used by Render)
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# ===========================================
# AUTHENTICATION
# ===========================================

# JWT Secret - MUST be changed in production!
# Use a long random string (64+ characters recommended)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# JWT token expiration
JWT_EXPIRES_IN=7d

# ===========================================
# CORS CONFIGURATION (Production)
# ===========================================

# Frontend URL for CORS (set in production)
# CORS_ORIGIN=https://systemscheck-web.onrender.com

# ===========================================
# OPTIONAL: Market Research Database
# ===========================================

# External database for CMS data lookups (optional)
# MARKET_DATABASE_URL=postgresql://user:password@host:5432/market_db
